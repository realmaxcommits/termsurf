[package]
name = "cef-test-launcher"
version = "0.1.0"
edition = "2021"
description = "cef-test launcher — XPC bootstrap service for endpoint exchange"

[[bin]]
name = "cef-test-launcher"
path = "src/main.rs"

[dependencies]
termsurf-xpc = { path = "../termsurf-xpc" }
